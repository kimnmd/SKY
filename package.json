{
  "name": "ee-probation-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve public",
    "build": "echo 'Static site — no build needed'"
  },
  "dependencies": {
    "firebase": "^12.11.0"
  }
}
